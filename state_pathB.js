function drawPathB() {
  background(139, 0, 0);
  fill(0);
  text("You choose to stay still.", width / 2, height / 2 - 20);
  text("The world becomes quiet.", width / 2, height / 2 + 10);
  text("Press SPACE to continue", width / 2, height / 2 + 50);
}
