function drawStart() {
  background(255, 200, 220); // pink
  fill(0);
  text("The blob wakes up in a pink world.", width / 2, height / 2 - 40);
  text("Press 1 to explore the light", width / 2, height / 2);
  text("Press 2 to stay where you are", width / 2, height / 2 + 40);
}
